package main

import (
	"github.com/gotneb/manga_api/api"
)

func main() {
	api.Init()
	/*
		manga, err := db.GetManga(db.MEUS_MANGAS, "One punch man")
		if err != nil {
			fmt.Println(err)
		}
		manga.Show()
	*/
}
